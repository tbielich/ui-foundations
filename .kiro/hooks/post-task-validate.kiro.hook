{
  "enabled": true,
  "name": "Validate After Task",
  "description": "Run CI check after completing a spec task to catch regressions immediately.",
  "version": "1",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "npm run ci:check"
  }
}