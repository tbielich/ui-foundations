# UI Foundations — Environment Variables
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.

# npm publish token (used by CI and .npmrc)
NPM_TOKEN=

# Figma API token (used by figma-developer-mcp)
FIGMA_TOKEN=
